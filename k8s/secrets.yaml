apiVersion: v1
kind: Secret
metadata:
  name: todo-app-secrets
type: Opaque
stringData:
  # Base64 encoded values
  # Generate: echo -n 'your-secret-key' | base64
  secret-key: "YOUR_BASE64_ENCODED_SECRET_KEY"
  # Database credentials if needed
  db-password: "YOUR_DB_PASSWORD"

